# 🏛️ Arquitetura da Aplicação - Diagramas e Fluxos

## Visão Geral da Arquitetura

```
                           REQUEST HTTP
                                ↓
        ┌─────────────────────────────────────────────┐
        │           LARAVEL ROUTING                   │
        │  (routes/web.php, routes/api.php)          │
        └─────────────────────────────────────────────┘
                                ↓
        ┌─────────────────────────────────────────────┐
        │        HTTP CONTROLLER                       │
        │  (Thin - apenas coordena)                   │
        │  Injeta Services                            │
        └─────────────────────────────────────────────┘
                                ↓
        ┌─────────────────────────────────────────────┐
        │        SERVICE LAYER                         │
        │  (Lógica de Negócio)                        │
        │  ├─ CursoService                            │
        │  ├─ StudentService                          │
        │  └─ RegistrationService                     │
        └─────────────────────────────────────────────┘
                                ↓
        ┌─────────────────────────────────────────────┐
        │        REPOSITORY LAYER                      │
        │  (Abstração de Dados)                       │
        │  ├─ CursoRepository                         │
        │  ├─ StudentRepository                       │
        │  └─ RegistrationRepository                  │
        └─────────────────────────────────────────────┘
                                ↓
        ┌─────────────────────────────────────────────┐
        │         ELOQUENT MODEL                       │
        │  (Banco de Dados)                           │
        │  ├─ Curso                                   │
        │  ├─ Student                                 │
        │  └─ Registration                            │
        └─────────────────────────────────────────────┘
                                ↓
        ┌─────────────────────────────────────────────┐
        │         MODEL OBSERVERS                      │
        │  (Event Listeners)                          │
        │  ├─ CursoObserver                           │
        │  ├─ StudentObserver                         │
        │  └─ RegistrationObserver                    │
        └─────────────────────────────────────────────┘
                      ↙         ↓         ↖
        ┌──────────────┐  ┌────────────────┐  ┌──────────────┐
        │    AUTH      │  │    CACHE      │  │   MAIL       │
        │   MANAGER    │  │   MANAGER     │  │   MANAGER    │
        │  (Singleton) │  │  (Singleton)  │  │  (Singleton) │
        └──────────────┘  └────────────────┘  └──────────────┘
                                ↓
        ┌─────────────────────────────────────────────┐
        │      NOTIFICATION ADAPTERS                   │
        │  (múltiplos canais)                         │
        │  ├─ EmailNotificationAdapter                │
        │  ├─ LogNotificationAdapter                  │
        │  └─ DatabaseNotificationAdapter             │
        └─────────────────────────────────────────────┘
                                ↓
                         RESPONSE HTTP
```

---

## Fluxo Detalhado: Criar Aluno

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    1. HTTP REQUEST: POST /students                       │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│           2. ROUTER: Route::post('/students', ...)                      │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    3. CONTROLLER: StudentController::store()                            │
│        ├─ FormRequest valida dados                                      │
│        ├─ Injeta StudentService                                         │
│        └─ Chama $service->createStudent($validated)                     │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    4. SERVICE: StudentService::createStudent()                          │
│        ├─ Chama $repository->create($data)                              │
│        ├─ Prepara notificação                                           │
│        └─ Return student                                                │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    5. REPOSITORY: StudentRepository::create()                           │
│        ├─ Valida dados novamente                                        │
│        ├─ Chama $model->create($data)                                   │
│        └─ Return model instance                                         │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    6. ELOQUENT MODEL: Student::create()                                 │
│        ├─ Executa Observers registered                                  │
│        └─ Persist no banco de dados                                     │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    7. OBSERVER: StudentObserver::created()                              │
│        ├─ CacheManager::invalidateUsers()                               │
│        │   └─ Remove 'users' key do cache                               │
│        ├─ MailManager::sendWelcome()                                    │
│        │   └─ Envia email de boas-vindas                                │
│        └─ Log::info("Student created...")                               │
│            └─ Registra evento em log                                    │
└─────────────────────────────────────────────────────────────────────────┘
                         ↙        ↓        ↖
┌────────────────┐  ┌────────────────┐  ┌──────────────────┐
│ CACHE MANAGER  │  │ MAIL MANAGER   │  │ NOTIFICATION MGR │
│ (Singleton)    │  │ (Singleton)    │  │ (Factory)        │
│                │  │                │  │                  │
│ put(            │  │ send(          │  │ sendMultiple(    │
│   'users',      │  │   email,       │  │   ['email',      │
│   $users,       │  │   'subject',   │  │    'database'],  │
│   3600          │  │   'view',      │  │   email,         │
│ )               │  │   data         │  │   subject,       │
│                 │  │ )              │  │   message        │
│ forget('users') │  │                │  │ )                │
│ remove cache    │  │ Facade: Mail:: │  │                  │
└────────────────┘  │ send()         │  │ EmailNotif...    │
                    │                │  │ DatabaseNotif... │
                    │ MailManager::  │  │ LogNotif...      │
                    │ getInstance()  │  └──────────────────┘
                    └────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    8. RESPONSE: Redirect com success message                            │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Fluxo Detalhado: Matricular Aluno

```
┌─────────────────────────────────────────────────────────────────────────┐
│         1. HTTP REQUEST: POST /registrations                            │
│            Body: {student_id: 1, curso_id: 2}                          │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    2. CONTROLLER: RegistrationController::store()                       │
│        ├─ FormRequest valida dados                                      │
│        ├─ Injeta RegistrationService                                    │
│        ├─ Try/Catch exceptions                                          │
│        └─ Chama $service->enrollStudent(1, 2)                           │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    3. SERVICE: RegistrationService::enrollStudent()                     │
│        ├─ Validação 1: Já está matriculado?                             │
│        │  └─ $repository->isEnrolled(1, 2) → false ✓                    │
│        ├─ Validação 2: Tem vagas?                                       │
│        │  └─ $curso->available_spots > 0 → true ✓                       │
│        ├─ Validação 3: Deadline aberto?                                 │
│        │  └─ $curso->registration_deadline > now() → true ✓             │
│        ├─ Criar matrícula                                               │
│        │  └─ $repository->create({...})                                 │
│        └─ Atualizar vagas                                               │
│           └─ $curso->decrement('available_spots')                       │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    4. REPOSITORY: RegistrationRepository::create()                      │
│        └─ Registration::create({...}) → novo registro                   │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    5. ELOQUENT MODEL: Registration::create()                            │
│        ├─ Persist no banco                                              │
│        ├─ Dispara observers                                             │
│        └─ Return model                                                  │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    6. OBSERVER: RegistrationObserver::created()                         │
│        ├─ CacheManager::invalidateUsers()                               │
│        ├─ Busca Student e Curso relacionados                            │
│        ├─ MailManager::sendRegistrationConfirmation()                   │
│        │  └─ Envia email com detalhes da matrícula                      │
│        ├─ NotificationManager::sendMultiple()                           │
│        │  ├─ EmailNotificationAdapter::send()                           │
│        │  │  └─ Envia via email                                         │
│        │  └─ DatabaseNotificationAdapter::send()                        │
│        │     └─ Salva notification em DB                                │
│        └─ Log::info("Registration created...")                          │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│    7. RESPONSE: Redirect com success message                            │
│       "Matrícula confirmada com sucesso!"                               │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Fluxo de Singleton Pattern

```
┌──────────────────────────────────────────────────────────────────────────┐
│                   PRIMEIRA CHAMADA                                        │
└──────────────────────────────────────────────────────────────────────────┘

$auth = AuthManager::getInstance()
  │
  ├─ if (self::$instance === null)  → TRUE
  │  └─ self::$instance = new AuthManager()  ← Cria instância
  │
  └─ return self::$instance

┌──────────────────────────────────────────────────────────────────────────┐
│                   SEGUNDA CHAMADA (mesmo lugar ou outro)                  │
└──────────────────────────────────────────────────────────────────────────┘

$auth2 = AuthManager::getInstance()
  │
  ├─ if (self::$instance === null)  → FALSE
  │  └─ (não entra)
  │
  └─ return self::$instance  ← Retorna MESMA instância

┌──────────────────────────────────────────────────────────────────────────┐
│                   RESULTADO                                               │
└──────────────────────────────────────────────────────────────────────────┘

$auth === $auth2 → TRUE ✓

┌──────────────────────────────────────────────────────────────────────────┐
│                   PROTEGIDO CONTRA CLONE                                  │
└──────────────────────────────────────────────────────────────────────────┘

// ❌ Não é possível clonar
$auth3 = clone AuthManager::getInstance()
  └─ ERROR: Call to private method __clone()

// ❌ Não é possível unserialize
unserialize(serialize($auth))
  └─ ERROR: Cannot unserialize singleton
```

---

## Fluxo de Adapter Pattern

```
┌──────────────────────────────────────────────────────────────────────────┐
│              NOTIFICATION MANAGER - FACTORY PATTERN                       │
└──────────────────────────────────────────────────────────────────────────┘

NotificationManager::new()
  │
  ├─ registerAdapters()
  │  ├─ register('email', new EmailNotificationAdapter())
  │  ├─ register('log', new LogNotificationAdapter())
  │  └─ register('database', new DatabaseNotificationAdapter())
  │
  └─ Ready to use

┌──────────────────────────────────────────────────────────────────────────┐
│                    USO: ENVIAR VIA ADAPTADOR PADRÃO                      │
└──────────────────────────────────────────────────────────────────────────┘

$notif->send(email, subject, message)
  │
  └─ $adapter = getDefault()
     │
     ├─ defaultAdapter ?? adapters['email'] ?? adapters['log']
     │
     └─ $adapter->send(...)
        └─ EmailNotificationAdapter::send(...)
           ├─ Mail::raw(...)
           └─ return true/false

┌──────────────────────────────────────────────────────────────────────────┐
│                    USO: ENVIAR VIA ADAPTADOR ESPECÍFICO                  │
└──────────────────────────────────────────────────────────────────────────┘

$notif->via('log')->send(email, subject, message)
  │
  ├─ adapter('log')  → LogNotificationAdapter
  │
  └─ $adapter->send(...)
     └─ Log::info(...)
        └─ return true

┌──────────────────────────────────────────────────────────────────────────┐
│                    USO: ENVIAR VIA MÚLTIPLOS ADAPTADORES                │
└──────────────────────────────────────────────────────────────────────────┘

$notif->sendMultiple(['email', 'log', 'database'], email, subject, message)
  │
  ├─ foreach (adapters as $adapter)
  │  │
  │  ├─ adapter('email')->send(...)     ✓ Email enviado
  │  ├─ adapter('log')->send(...)       ✓ Log registrado
  │  └─ adapter('database')->send(...)  ✓ Salvo em DB
  │
  └─ return [
       'email' => true,
       'log' => true,
       'database' => true
     ]

┌──────────────────────────────────────────────────────────────────────────┐
│                    ADICIONAR NOVO ADAPTADOR                              │
└──────────────────────────────────────────────────────────────────────────┘

// 1. Criar class (implementa NotificationAdapterInterface)
class SlackNotificationAdapter implements NotificationAdapterInterface { ... }

// 2. Registrar
$notif->register('slack', new SlackNotificationAdapter());

// 3. Usar
$notif->via('slack')->send(email, subject, message);

// SEM MODIFICAR código existente! 🎉
```

---

## Hierarquia de Repositórios

```
                  RepositoryInterface
                          △
                          │
                          │ implements
                          │
                  BaseRepository
                   △      △      △
                   │      │      │
      extends      │      │      │
        ┌──────────┴──┬───┴──────┴────┐
        │             │                │
CursoRepository  StudentRepository  RegistrationRepository

├─ all()                    ├─ all()                      ├─ all()
├─ find()                   ├─ find()                     ├─ find()
├─ findBy()                 ├─ findBy()                   ├─ findBy()
├─ paginate()               ├─ paginate()                 ├─ paginate()
├─ create()                 ├─ create()                   ├─ create()
├─ update()                 ├─ update()                   ├─ update()
├─ delete()                 ├─ delete()                   ├─ delete()
├─ deleteMany()             ├─ deleteMany()               ├─ deleteMany()
├─ search()                 ├─ search()                   ├─ search()
├─ filter()                 ├─ filter()                   ├─ filter()
├─ count()                  ├─ count()                    ├─ count()
├─ exists()                 ├─ exists()                   ├─ exists()
│                           │                            │
├─ withStudentCount()       ├─ withRegistrationsCount() ├─ withRelations()
├─ getAvailable()           ├─ withCourses()            ├─ getByStudent()
├─ getByType()              ├─ getActive()              ├─ getByCurso()
└─ getActive()              └─ getByEmail()             └─ isEnrolled()
```

---

## Hierarquia de Services

```
                   BUSINESS LOGIC LAYER
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   CursoService      StudentService    RegistrationService
        │                  │                  │
        ├─ Injeta          ├─ Injeta          ├─ Injeta
        │  CursoRepository │  StudentRepository
        │  NotificationMgr │  NotificationMgr │  RegistrationRepository
        │  CacheManager    │  CacheManager    │  CursoRepository
        │                  │                  │  NotificationMgr
        │                  │                  │  CacheManager
        │                  │                  │
        ├─ getAllCursos()  ├─ getAllStudents()├─ enrollStudent()
        ├─ getCursoById()  ├─ getStudentById()├─ cancelEnrollment()
        ├─ createCurso()   ├─ createStudent() ├─ getByStudent()
        ├─ updateCurso()   ├─ updateStudent() ├─ getByCurso()
        ├─ deleteCurso()   ├─ deleteStudent() └─ isEnrolled()
        ├─ searchCursos()  └─ searchStudents()
        └─ deleteCursos()
```

---

## Ciclo de Vida de uma Matrícula

```
┌────────────────────────────────────────────────────────────────────────┐
│ CRIAR MATRÍCULA (POST /registrations)                                  │
└────────────────────────────────────────────────────────────────────────┘

1. Controller recebe FormRequest validado
2. Service valida negócio (não duplicado, vagas, deadline)
3. Repository cria registration em BD
4. Model dispara Observer
5. Observer:
   - Invalida cache de usuários
   - Envia email via MailManager
   - Envia notificação múltiplos canais
   - Registra log de evento
6. Responde ao cliente

┌────────────────────────────────────────────────────────────────────────┐
│ CANCELAR MATRÍCULA (DELETE /registrations/{id})                        │
└────────────────────────────────────────────────────────────────────────┘

1. Controller recebe ID
2. Service busca registration via Repository
3. Service incrementa available_spots no curso
4. Service deleta via Repository
5. Model dispara Observer
6. Observer:
   - Invalida cache de usuários
   - Envia notificação de cancelamento
   - Registra log de evento
7. Responde ao cliente

┌────────────────────────────────────────────────────────────────────────┐
│ RESULTADO FINAL                                                         │
└────────────────────────────────────────────────────────────────────────┘

                  ┌─────────────────────────────────┐
                  │   REGISTRATION TABLE            │
                  ├─────────────────────────────────┤
                  │ id │ student_id │ curso_id │ ... │
                  ├────┼────────────┼──────────┼─────┤
                  │ 1  │      1     │    2     │ ... │
                  └─────────────────────────────────┘
                           ↑
         Criado por RegistrationService::enrollStudent()
         Observado por RegistrationObserver::created()

                  ┌─────────────────────────────────┐
                  │   NOTIFICATIONS TABLE           │
                  ├─────────────────────────────────┤
                  │ id │ recipient │ subject │ ... │
                  ├────┼───────────┼─────────┼─────┤
                  │ 1  │ ...@... │ Matríc. │ ... │
                  └─────────────────────────────────┘
                           ↑
         Criado por DatabaseNotificationAdapter
         Via RegistrationObserver::created()

                  ┌─────────────────────────────────┐
                  │   MAIL QUEUE / SENT             │
                  ├─────────────────────────────────┤
                  │ to │ subject │ template │ ... │
                  ├────┼─────────┼──────────┼─────┤
                  │... │ Matríc. │ ...conf  │ ... │
                  └─────────────────────────────────┘
                           ↑
         Enviado por MailManager::sendRegistrationConfirmation()
         Via RegistrationObserver::created()

                  ┌─────────────────────────────────────┐
                  │   CACHE (INVALIDATED)               │
                  ├─────────────────────────────────────┤
                  │ Key: 'users' → ❌ REMOVED           │
                  │ Key: 'all_cursos' → ❌ REMOVED      │
                  │ Key: 'all_students' → ❌ REMOVED    │
                  └─────────────────────────────────────┘
                           ↑
         Invalidado por CacheManager::invalidateUsers()
         Via RegistrationObserver::created()

                  ┌─────────────────────────────────────┐
                  │   LOGS                              │
                  ├─────────────────────────────────────┤
                  │ [INFO] Registration created:        │
                  │ Student (id:1) enrolled in (id:2)   │
                  └─────────────────────────────────────┘
                           ↑
         Registrado por RegistrationObserver::created()
```

---

## Benefícios Visuais

```
┌─────────────────────────────────────────────────────────────────────────┐
│  ❌ ANTES (Sem Padrões)                                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  Controller                         Model                                │
│  ├─ Validação                      ├─ Validação                          │
│  ├─ Query ao BD                    └─ Persist                            │
│  ├─ Lógica de negócio                                                    │
│  ├─ Manipulação dados                                                    │
│  ├─ Envio de email                                                       │
│  ├─ Cache invalidation                                                   │
│  └─ Logging                                                              │
│                                                                           │
│  ⚠️  Problemas:                                                          │
│      - 200+ linhas por controller                                        │
│      - Lógica acoplada                                                   │
│      - Difícil testar                                                    │
│      - Não reutilizável                                                  │
│                                                                           │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  ✅ DEPOIS (Com Padrões)                                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  Controller          Service          Repository       Model   Observer  │
│  ├─ HTTP            ├─ Negócio       ├─ Query        ├─ ORM   ├─ Cache  │
│  ├─ Validação       ├─ Validações    ├─ Abstração    └─ BD    ├─ Email  │
│  └─ Resposta        ├─ DI            └─ Interface       ↑      └─ Log    │
│                     └─ Reutilizável                 Observers           │
│                                                                           │
│  ✨ Benefícios:                                                          │
│      - 10-20 linhas por controller                                       │
│      - Lógica desacoplada                                                │
│      - Fácil testar                                                      │
│      - Reutilizável em múltiplos contextos                               │
│      - Escalável                                                         │
│      - Manutenível                                                       │
│                                                                           │
└─────────────────────────────────────────────────────────────────────────┘
```

---

**Última atualização**: 19 de Fevereiro de 2026

Para mais detalhes, ver:
- [DESIGN_PATTERNS.md](DESIGN_PATTERNS.md) - Documentação completa
- [DESIGN_PATTERNS_QUICK_GUIDE.md](DESIGN_PATTERNS_QUICK_GUIDE.md) - Guia rápido
- [REFACTORING_GUIDE.md](REFACTORING_GUIDE.md) - Como refatorar controllers
